{
  "name": "hacklauncher-frontend",
  "private": true,
  "version": "1.1.4",
  "type": "module",
  "scripts": {
    "build:css": "tailwindcss -c ./tailwind.config.cjs -i ./public/assets/css/tailwind-input.css -o ./public/assets/css/tailwind.css --minify",
    "dev": "npm run build:css && vite",
    "build": "npm run build:css && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17",
    "vite": "^3.2.11"
  }
}
